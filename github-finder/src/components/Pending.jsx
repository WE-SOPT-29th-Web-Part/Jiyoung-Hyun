import React from "react";
import Styled from "styled-components";

const Pending = () => {
  return <SRoot>Loading...</SRoot>;
};

export default Pending;

const SRoot = Styled.p`
  font-family: 'ZenAntique';
  font-size: 36px;
`;
